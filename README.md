# hello-world → LEDGER trading journal

This repo now hosts **LEDGER**, a Bitget-connected BTC ICT trading journal.

```bash
cd journal
cp .env.example .env.local   # add Bitget keys when ready
npm install
npm run dev
```

See [`journal/README.md`](journal/README.md) for setup, env vars, and review workflow.
